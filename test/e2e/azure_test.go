// +build azure

package e2e

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("Testing CSI Driver with Azure provider", func() {
	It("Should create a workload cluster for azure", func() {
		By("azure")
	})
})
