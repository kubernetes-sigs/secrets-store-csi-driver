---
apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: "{{ .Namespace }}"
  labels:
    app: vault
spec:
  type: NodePort
  selector:
    app: vault
  ports:
  - name: vault-port
    port: 8200
    targetPort: 8200
    protocol: TCP
