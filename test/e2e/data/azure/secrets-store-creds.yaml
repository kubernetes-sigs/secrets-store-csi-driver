---
apiVersion: v1
kind: Secret
metadata:
  name: secrets-store-creds
  namespace: "{{ .Namespace }}"
  labels:
    app: vault
data:
  clientid: "{{ .ClientID }}"
  clientsecret: "{{ .ClientSecret }}"
