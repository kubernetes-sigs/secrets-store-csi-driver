version: "2"
run:
  go: "1.26"
linters:
  default: none
  enable:
    - copyloopvar
    - durationcheck
    - errcheck
    - errorlint
    - exhaustive
    - goconst
    - gocritic
    - gocyclo
    - gosec
    - govet
    - ineffassign
    - makezero
    - misspell
    - nilerr
    - revive
    - staticcheck
    - unused
    - whitespace
  settings:
    gocritic:
      enabled-tags:
        - performance
    misspell:
      locale: US
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - gosec
          # Hardcoded cryptographic materials is ok in tests
        text: "G101:"
        path: _test\.go
      - linters:
          # Repetition in tests is at the author's discretion.
          - goconst
        path: _test\.go
      - linters:
          - revive
        text: 'unused-parameter: parameter ''.*'' seems to be unused, consider removing or renaming it as _'
      - linters:
          - revive
        text: 'dot-imports: should not use dot imports'
        source: . "github.com/onsi/(ginkgo/v2|gomega)"
    paths:
      - zz_generated.*\.go$
      - pkg/util/fileutil/atomic_writer.go
      - pkg/util/fileutil/atomic_writer_test.go
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
severity:
  default: error
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - zz_generated.*\.go$
      - pkg/util/fileutil/atomic_writer.go
      - pkg/util/fileutil/atomic_writer_test.go
      - third_party$
      - builtin$
      - examples$
